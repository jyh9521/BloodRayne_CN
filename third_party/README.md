Place optional third-party binaries here if you want build.ps1 to include them in release output.

Currently supported:
- winmm.dll: optional compatibility DLL copied to release root when present.

Do not commit commercial game files or third-party binaries unless their license allows redistribution.
